import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 45,
    borderColor: 'gray',
    borderWidth: 1,
    marginHorizontal: 20,
    margin: 10,
    paddingLeft: 10,
    borderRadius: 8,
    marginTop: 5,
  },
  label: {
    marginTop: 10,
    marginStart: 20,
    fontSize: 18,
  }
});
